import Split from "split.js"

//const defaultPaneOptions = {}

//const defaultOptions = {
//percent: true
//};

class SplitAbsolute {
  constructor(panes, options) {
    //panes = normalizePaneOptions([...panes]);
    //options = {...defaultOptions, options};
  }
}

export default SplitAbsolute;
